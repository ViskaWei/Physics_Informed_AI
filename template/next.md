请现在用几分钟总结一下昨天的实验进度，按这个结构过一遍：

昨天跑了什么实验？

实验编号 / 配置（模型、超参、数据版本等）

关键改动（和前一次相比改了什么）

关键结果是什么？

核心指标（例如：accuracy / loss / throughput / latency / stability 等）

是否优于对照组 / baseline？幅度大概多少？

有无异常现象（发散、不收敛、数值爆炸、奇怪分布等）

得到的结论 / 观察：

哪些假设被支持？

哪些看起来是无效方向？

有什么需要进一步验证的现象？

自然推导出的下一步（next steps）：

保留 & 精修的方向（继续精调哪些超参 / 架构）

需要新增的对照组 / 消融实验

需要补的分析（可视化 / 日志排查 / 理论推导）

然后基于上面的总结，今天的实验计划可以这样定：

✅ 选出 1–2 个最有前景的方向，作为今天的主线

✅ 明确每个方向今天要跑的 具体实验编号 / 配置组合

✅ 给每个主实验设一个简单的 “成败判据”（例如：> baseline +1% 或收敛速度提升 20%）

✅ 预留时间做 结果分析而不是只排队开新实验