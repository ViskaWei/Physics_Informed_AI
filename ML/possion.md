这道题的正确答案是 **C. 事件发生的时间间隔服从正态分布**。

这一项**不是**泊松过程的特征。泊松过程中的事件间隔服从的是**指数分布**（对应选项 A），而不是正态分布。

下面用数学公式为您详细推导和解释每一个特征：

### 1. 为什么选 C（以及为什么 A 是对的）？

我们来推导一下“事件发生的时间间隔”服从什么分布。

假设泊松过程的速率参数为 （表示单位时间内平均发生的事件次数）。
设  为第 1 个事件发生的时间（即从 0 到第 1 个事件的时间间隔）。

如果 ，意味着在时间段  内发生的事件数量  为 0。
根据泊松分布的定义（见下文第 3 点）：


当  时：


那么， 的**累积分布函数 (CDF)** 为：


对  求导得到**概率密度函数 (PDF)**：


**结论：**

* 上式正是**指数分布 (Exponential Distribution)** 的公式。
* 所以，**选项 A 正确**（是特征），而 **选项 C 错误**（不是特征，正态分布的公式完全不同）。

---

### 2. 解释选项 B：独立增量性

**特征描述：** 事件在不重叠时间区间内相互独立。

**数学表达：**
对于任意的时间点 ，
时间区间  内发生的事件数 
与时间区间  内发生的事件数 
是**相互独立的随机变量**。

这意味着过去发生的事情不会影响未来事件在独立时间段内的发生概率（这也是“无记忆性”的一种体现）。

---

### 3. 解释选项 D：计数服从泊松分布

**特征描述：** 每个时间区间内事件发生的次数服从泊松分布。

**数学表达：**
这是泊松过程最基本的定义。对于长度为  的时间区间（例如 ），该区间内发生  次事件的概率由泊松分布给出：

其中：

*  是泊松过程的速率（强度）。
*  是时间区间的长度。
*  是该区间内期望发生的事件总数。

### 总结

* **A (指数分布)**：✅ 正确，间隔时间服从 。
* **B (相互独立)**：✅ 正确，这是泊松过程的独立增量假设。
* **C (正态分布)**：❌ **错误**，间隔不是正态分布。
* **D (泊松分布)**：✅ 正确，单位时间内的次数服从泊松分布。

因此，**选项 C** 是这一题的答案。