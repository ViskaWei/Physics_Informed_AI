# 2025-11-19 AI 方向题

## 一、单选题

### 1. 在 Transformer 的最后一层，Softmax 作为“激活层”的核心意义是下列哪一项？

* A. 自动选择最大 logit 对应的类别索引，并输出整数组标签
* B. 对 logits 进行非线性放大，从而增大不同类别之间的数值差异
* C. 将任意实数向量转换成相加后和为 1 的概率分布，使模型输出可直接解释为类别置信度
* D. 将输出映射到 (−1,1) 区间，避免梯度消失

**答案：C**

---

### 2. 给定数据：x=[1,2,3]，y=[1,4,9]。用最小二乘法拟合直线 y=a+bx，则系数 b 为？

* A. 2.5
* B. 3.0
* C. 4.0
* D. 3.5

**答案：C**

---

### 3. 连续随机变量 X 的概率密度函数为 f(x)=2x，其中 0≤x≤1，那么 P(0.5≤X≤1) 为？

* A. 0.5
* B. 1
* C. 0.25
* D. 0.75

**答案：D**

---

### 4. 以下关于自注意力机制中多头注意力（Multi-Head Attention MHA）、多查询注意力机制（Multi-Query Attention MQA）、分组查询注意力机制（Grouped Query Attention GQA）、多头潜在注意力（Multi-Head Latent Attention MLA）描述错误的是？

* A. MQA 核心思想是共享键（Key）和值（Value）的投影参数，仅对查询（Query）使用独立的头参数。
* B. MLA 核心思想是通过低秩投影压缩查询（Q）、键（K）、值（V）的维度，并在注意力计算中解耦内容与位置信息，从而减少计算复杂度，同时保留长距离依赖建模能力。
* C. MHA 核心思想是将输入映射到多个子空间，分别计算注意力权重并聚合结果，从而增强模型对复杂模式的捕捉能力。
* D. GQA 是对多头注意力（MHA）和多头潜在注意力（MLA）的折中优化方案。

**答案：D**

---

### 5. 在反向传播过程中，如果发现某个特定层的学习速度显著慢于其他层，以下选项中，最可能的原因是？

* A. 该层的参数数量较少
* B. 数据标准化不充分
* C. 学习率设置过低
* D. 该层的梯度消失

**答案：D**

---

### 6. ROC曲线的主要用途是什么？

* A. 展示回归模型的预测误差分布
* B. 计算分类模型的F1分数
* C. 计算分类模型的准确率
* D. 评估分类模型在不同阈值下的真正性率和假阳性率表现

**答案：D**

---

### 7. 在数值计算中，截断误差的主要来源是什么？

* A. 数学模型近似（如用有限项近似无穷级数）导致的误差
* B. 计算机有限的存储精度导致的误差
* C. 算法迭代过程中的随机误差
* D. 数据输入时的测量误差

**答案：A**

---

### 8. 如果随机变量 X 和 Y 的期望满足 E(XY)=E(X)E(Y)，则下式哪项成立

* A. Var(X+Y)=Var(X)+Var(Y)
* B. Var(X^2+Y^2)=Var(X^2)+Var(Y^2)
* C. Var(X−Y)=Var(X)−Var(Y)
* D. Var(XY)=Var(X)Var(Y)

**答案：A**

---

### 9. 设总体 X∼N(μ,σ^2)，X1,X2,X3 是来自 X 的简单随机样本，则 Cov(X1+X2,X3)=（ ）注：简单随机样本指 X1,X2,X3 独立同分布，且每个 Xi 与总体 X 同分布

* A. 0
* B. σ^2
* C. 2σ^2
* D. σ^2/3

**答案：A**

---

### 10. 朴素贝叶斯分类器的特点是（ ）

* A. 假设样本各维属性存在依赖
* B. 假设样本服从多项式分布
* C. 假设样本各维属性独立
* D. 假设样本服从正态分布

**答案：C**

---

### 11. 在训练超大规模语言模型时，为了在显存受限的情况下仍能使用较深的网络并保持梯度正确，常用的内存优化技术是

* A. 随机失活（Dropout）
* B. 梯度裁剪（Gradient Clipping）
* C. 激活检查点（Activation Checkpointing）
* D. 权重衰减（Weight Decay）

**答案：C**

---

### 12. 某二分类模型混淆矩阵为：TP=80，TN=100，FP=20，FN=50，其F1分数约为？

* A. 0.78
* B. 0.54
* C. 0.7
* D. 0.62

**答案：C**

---

### 13. 在多层感知器（MLP）中，隐藏层通过一系列非线性变换（由激活函数实现）对输入数据进行变化操作，这样的主要作用是什么？

* A. 直接映射输入到输出，无需中间处理
* B. 学习输入特征之间的复杂关系，并进行非线性变换
* C. 确保模型的预测结果为线性可分
* D. 用于减少模型的训练时间

**答案：B**

---

### 14. 对线性方程组 Ax=b，其中 A 为 3×4 矩阵，rank(A)=rank([A∣b])=3，这里 [A∣b] 表示增广矩阵，则该方程组的解的情况是：

* A. 无穷多解，且含 2 个自由变量
* B. 无解
* C. 唯一解
* D. 无穷多解，且含 1 个自由变量

**答案：D**

---

### 15. 设 A 为 n(n≥2) 阶非零矩阵，且满足 aij=Aij, i,j=1,2,…,n，其中 Aij 为 aij 的代数余子式，则下列选项中正确的是（ ）

* A. A 对称
* B. A 可逆
* C. ∣A∣<0
* D. A 的所有元素的平方和为 n

**答案：B**

---

## 二、多选题

### 16. 在大模型预训练过程中，以下哪几项技术通常被用来提高模型的训练效率和稳定性？

* A. 采用混合精度训练（Mixed Precision Training）
* B. 使用 FP32 以提高精度
* C. 使用多种并行策略分配计算负载
* D. 使用动态学习率调度

**答案：A、C、D**

* ✅ A：混合精度可提速降显存，常配合 loss scaling。
* ❌ B：纯 FP32 通常更慢更耗显存，不是“提高效率”的常用手段。
* ✅ C：数据/张量/流水线等并行可分摊计算与显存压力。
* ✅ D：学习率调度提升稳定性与收敛效率。

---

### 17. 下列关于 BERT 与 GPT 两种 Transformer 模型在预训练目标、架构设计及适用场景等方面的说法，哪些是正确的？

* A. GPT 原生支持从左到右的文本生成与填空续写，BERT 由于掩码策略在端到端生成任务上能力有限。
* B. BERT 的双向上下文表示更适合句子级分类、序列标注等理解任务，GPT 的单向生成能力更适合续写与对话生成。
* C. BERT 采用掩码语言模型（Masked LM）+ 下一句预测（Next Sentence Prediction），GPT 采用自回归语言模型（Auto-Regressive LM）。
* D. BERT 输入时加入 token type 嵌入用于区分句子对，GPT 省略了 token type 嵌入，仅依赖位置与词嵌入。

**答案：A、B、C、D**

* ✅ A：自回归天然生成；MLM 对端到端生成不友好。
* ✅ B：双向表征更适合理解；单向更适合生成。
* ✅ C：预训练目标差异：MLM(+NSP) vs AR。
* ✅ D：BERT 有 token type；GPT 通常没有。

---

### 18. 在使用强化学习（RL）对大语言模型（LLM）进行对齐（Alignment），例如 RLHF 中，以下哪些说法是正确的？

* A. 强化学习微调阶段的目标是直接最小化模型输出与人类标注的“完美”回答之间的交叉熵损失。
* B. 在 PPO 算法的应用中，策略模型（即 LLM）的更新目标是最大化由奖励模型给出的奖励以及最小化当前策略模型与初始 SFT 模型之间的 KL 距离。
* C. 通常使用一个独立的“奖励模型”（Reward Model, RM）来代替环境，为 LLM（作为智能体）的输出提供奖励信号。
* D. “冷启动”（Cold Start）问题指的是在没有人类标注数据的情况下，直接用强化学习训练 LLM 会非常困难，因此通常需要先进行监督微调（SFT）。

**答案：B、C、D**

* ❌ A：RLHF 的 RL 阶段通常是“最大化奖励 + KL 约束”，不是直接 CE。
* ✅ B：PPO 目标兼顾奖励与 KL 正则（约束偏离 SFT）。
* ✅ C：RM 给奖励信号，充当环境/偏好函数的代理。
* ✅ D：先 SFT 解决冷启动，再 RL 对齐。

---

### 19. 在卷积神经网络中，哪些层是最常见的？

* A. 卷积层
* B. 全连接层
* C. 循环层
* D. 池化层

**答案：A、D**

* ✅ A：CNN 核心结构。
* ❌ B：常见但不是“最基础/最常见必备”（且现代架构末端可用 GAP 等替代）。
* ❌ C：RNN 不是 CNN 典型组件。
* ✅ D：池化层经典 CNN 常见模块。

---

### 20. 关于 PCA，以下正确的是：

* A. 特征向量按特征值排序
* B. 可直接捕捉非线性结构
* C. 最大化样本在新坐标系的方差
* D. 是一种线性降维方法

**答案：A、C、D**

* ✅ A：按特征值从大到小取主成分。
* ❌ B：PCA 本质线性，非线性需核 PCA 等。
* ✅ C：主成分方向最大化投影方差。
* ✅ D：线性降维方法。
